#include<stdio.h>

int main(){

    // arithmatic operators
    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus) // returns remainder
    // ++ (increment)
    // -- (decrement)


    int x = 7;
    int y = 2;

    // int z = x + y;
    // int z = x - y;
    // int z = x * y;
    // float z = x /(float) y;
    // int z = x % y;
    // x++;
    // y--;

    printf("%d", y);
    return 0;
}
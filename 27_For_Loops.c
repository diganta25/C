#include<stdio.h>

int main(){

    // for loops = repeats a section of code after limited amount of times

    //for(int i = 1; i <= 10; i+=2)     // index = i
    for(int i = 10; i >= 1; i--)        //decrement
    {
        printf("\n%d", i);
    }

    return 0;
}
#include<stdio.h>

int main(){
    // constant = fixed value that can't be altered by the program during its execution

    const float PI = 3.14159;
    // PI = 225.25; 
    
    printf("%f", PI);


    return 0;
}